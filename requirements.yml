---
# Install with: ansible-galaxy collection install -r requirements.yml

collections:
  - name: ansible.posix
    version: ">=1.4.0"
  - name: community.general
    version: ">=6.0.0"
  # OCI Dynamic Inventory (optional - only needed if using inventory.oci.yml)
  - name: oracle.oci
    version: ">=5.0.0"
